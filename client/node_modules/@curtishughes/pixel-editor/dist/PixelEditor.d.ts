import History from './History';
import { Pixel, Tool } from './types';
export default class PixelEditor {
    private _canvas;
    readonly width: number;
    readonly height: number;
    tool: Tool;
    history: History;
    private _context;
    private _previousPosition;
    private _pixels;
    constructor(_canvas: HTMLCanvasElement, width: number, height: number, tool: Tool, history?: History);
    private mouseup;
    private mousedown;
    private mousemove;
    private touchstart;
    private touchmove;
    private touchPosition;
    private mousePosition;
    get(x: number, y: number): Pixel;
    set(pixels: Pixel[], logToHistory?: boolean): void;
    get pixels(): Pixel[];
    clear(): void;
    undo(): void;
    redo(): void;
}
