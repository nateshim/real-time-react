import { Pixel } from './types';
export default class PixelCollection {
    width: number;
    private pixels;
    constructor(width: number);
    set({ x, y, color }: Pixel): void;
    get(x: number, y: number): string | undefined;
    private getIndex;
    private getPixel;
    [Symbol.iterator](): Generator<{
        x: number;
        y: number;
        color: string | undefined;
    }, void, unknown>;
}
