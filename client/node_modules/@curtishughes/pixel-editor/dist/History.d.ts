import { Delta, Pixel } from './types';
export default class History {
    private maxLength;
    private undoStack;
    private redoStack;
    constructor(maxLength?: number, undoStack?: Delta[], redoStack?: Delta[]);
    push(delta: Delta): void;
    prev(): Pixel[] | undefined;
    next(): Pixel[] | undefined;
    squash(): void;
}
